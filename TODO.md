- [ ] Look at migrating off of lazy instead for `vim.pack`. Does it really not have lazy loading?
- [ ] Review plugins against what `snacks` provides, as well as what each of what `snacks` provides.
- [ ] Review `Zen` mode plugin because it's not reall that "zen", tbh. It's just margins.

